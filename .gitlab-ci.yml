image: python:3.11

pages:
  stage: deploy
  script:
    - pip install mkdocs mkdocs-material mkdocs-static-i18n[material]
    - mkdocs build --site-dir public
  artifacts:
    paths:
      - public
  only:
    - main
